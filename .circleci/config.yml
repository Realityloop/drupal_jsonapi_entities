version: 2

defaults: &defaults
  docker:
  - image: circleci/node:8.9.1

jobs:
  test:
    <<: *defaults
    steps:
      - checkout

      - run: npm install
      - run:
          name: Run tests
          command: npm test
